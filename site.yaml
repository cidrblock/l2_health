- hosts: all
  gather_facts: false
  collections:
  - nmake.jetpack
  tasks:
  - include_role:
      name: collect
    vars:
      collect:
        nxos:
        - '!interfaces'
        eos:
        - '!spanning_tree'
  - debug:
      var: ansible_facts
